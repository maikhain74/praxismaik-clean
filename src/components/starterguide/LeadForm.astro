<section
  id="starterguide-formular"
  class="lead-form"
  aria-labelledby="lead-form-title"
>
  <div class="lead-form__box">
    <span class="lead-form__badge">
      Kostenlos herunterladen
    </span>

    <h2 id="lead-form-title">
      Kostenlosen StarterGuide erhalten
    </h2>

    <p class="lead-form__text">
      Trage deine E-Mail-Adresse ein und erhalte den StarterGuide
      <strong>„Die 10 häufigsten Fehler im Praxisbesuch“</strong>
      kostenlos per E-Mail.
    </p>

    <div class="lead-form__benefits" aria-label="Vorteile des StarterGuides">
      <div>
        <strong>35 Seiten</strong>
        <span>Praxisnah und verständlich</span>
      </div>

      <div>
        <strong>Sofort verfügbar</strong>
        <span>Direkt nach der Bestätigung</span>
      </div>

      <div>
        <strong>100 % kostenlos</strong>
        <span>Ohne Verpflichtung</span>
      </div>
    </div>

    <div class="lead-form__embed">
      <div class="ml-embedded" data-form="UeK3hq"></div>
    </div>

    <p class="lead-form__privacy">
      Kein Spam. Du kannst dich jederzeit mit einem Klick wieder abmelden.
    </p>
  </div>
</section>

<script is:inline>
  (function (w, d, e, u, f, l, n) {
    w[f] =
      w[f] ||
      function () {
        (w[f].q = w[f].q || []).push(arguments);
      };

    l = d.createElement(e);
    l.async = 1;
    l.src = u;

    n = d.getElementsByTagName(e)[0];
    n.parentNode.insertBefore(l, n);
  })(
    window,
    document,
    "script",
    "https://assets.mailerlite.com/js/universal.js",
    "ml"
  );

  ml("account", "2503547");
</script>

<style>
  .lead-form {
    display: flex;
    justify-content: center;
    padding: clamp(4rem, 8vw, 7rem) 0;
    scroll-margin-top: 2rem;
  }

  .lead-form__box {
    width: 100%;
    max-width: 820px;
    padding: clamp(2rem, 5vw, 3.75rem);
    border: 1px solid #efe0cf;
    border-radius: 1.75rem;
    background: #ffffff;
    box-shadow: 0 20px 60px rgb(68 45 27 / 8%);
    text-align: center;
  }

  .lead-form__badge {
    display: inline-flex;
    margin-bottom: 1.4rem;
    padding: 0.55rem 1rem;
    border-radius: 999px;
    background: #fff3e8;
    color: #b85e17;
    font-size: 0.94rem;
    font-weight: 800;
  }

  .lead-form h2 {
    max-width: 680px;
    margin: 0 auto;
    color: #173850;
    font-size: clamp(2rem, 4.5vw, 3.25rem);
    line-height: 1.1;
    letter-spacing: -0.035em;
  }

  .lead-form__text {
    max-width: 660px;
    margin: 1.3rem auto 0;
    color: #4a4a4a;
    font-size: clamp(1rem, 1.8vw, 1.15rem);
    line-height: 1.7;
  }

  .lead-form__benefits {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1rem;
    margin: 2rem 0 2.25rem;
  }

  .lead-form__benefits div {
    display: grid;
    gap: 0.25rem;
    padding: 1.15rem;
    border: 1px solid #f0dfcf;
    border-radius: 1rem;
    background: #fff8f1;
  }

  .lead-form__benefits strong {
    color: #e67e22;
    font-size: 1.02rem;
  }

  .lead-form__benefits span {
    color: #555555;
    font-size: 0.88rem;
    line-height: 1.45;
  }

  .lead-form__embed {
    max-width: 600px;
    margin: 0 auto;
  }

  .lead-form__privacy {
    margin: 1.2rem 0 0;
    color: #686868;
    font-size: 0.9rem;
    line-height: 1.5;
  }

  .lead-form__embed :global(.ml-form-embedWrapper) {
    width: 100% !important;
    max-width: none !important;
    background: transparent !important;
  }

  .lead-form__embed :global(.ml-form-embedBody) {
    padding: 0 !important;
  }

  .lead-form__embed :global(.ml-form-embedContent) {
    display: none !important;
  }

  .lead-form__embed :global(.ml-form-formContent) {
    margin-bottom: 0 !important;
  }

  .lead-form__embed :global(.ml-form-fieldRow) {
    margin-bottom: 0.9rem !important;
  }

  .lead-form__embed :global(input[type="email"]) {
    min-height: 3.6rem !important;
    padding: 0.95rem 1rem !important;
    border: 1px solid #d9c7b6 !important;
    border-radius: 0.85rem !important;
    background: #ffffff !important;
    color: #173850 !important;
    font: inherit !important;
    font-size: 1rem !important;
  }

  .lead-form__embed :global(input[type="email"]:focus) {
    border-color: #e67e22 !important;
    outline: 3px solid rgb(230 126 34 / 16%) !important;
  }

  .lead-form__embed :global(.ml-form-embedSubmit) {
    margin: 0 !important;
  }

  .lead-form__embed :global(.ml-form-embedSubmit button.primary) {
    min-height: 3.65rem !important;
    padding: 0.95rem 1.25rem !important;
    border-radius: 0.85rem !important;
    background: #e67e22 !important;
    color: #ffffff !important;
    font: inherit !important;
    font-size: 1.02rem !important;
    font-weight: 850 !important;
    transition:
      background-color 160ms ease,
      transform 160ms ease !important;
  }

  .lead-form__embed :global(.ml-form-embedSubmit button.primary:hover) {
    transform: translateY(-1px);
    background: #d96f16 !important;
  }

  .lead-form__embed :global(.ml-form-successBody) {
    padding: 1.5rem !important;
    border: 1px solid #efddcc;
    border-radius: 1rem;
    background: #fff8f1;
  }

  @media (max-width: 680px) {
    .lead-form__benefits {
      grid-template-columns: 1fr;
    }
  }
</style>